export const validateID = (id: string): boolean => {
  console.log(id);
  return id !== '';
};
export const validateName = (name: string): boolean => name !== '';
export const validateType = (type: string): boolean => type !== '';
export const validateIP = (ip: string): boolean => ip !== '';
